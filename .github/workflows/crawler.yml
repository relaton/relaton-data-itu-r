# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
name: Crawler

on:
  push:
    branches: [ master, main ]
    tags: [ v* ]
  pull_request:
  # Daily crawl paused: ITU decommissioned the RunSearch enumeration endpoint and
  # Relaton::Itu::DataFetcher.fetch now raises. See relaton/relaton#75. workflow_dispatch
  # is kept so the crawler can still be run manually if a source is restored.
  # schedule:
  # - cron: '0 14 * * *'
  workflow_dispatch:
    inputs:
      args:
        description: Extra args to pass to crawler.rb script
        required: false
        default: ''

jobs:
  crawl:
    uses: relaton/support/.github/workflows/crawler.yml@main
    with:
      args: ${{ github.event.inputs.args }}